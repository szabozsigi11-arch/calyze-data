# A nyilvános állapotoldal (mérési napló) újraépítése és közzététele.
#
# Adatot nem közöl: csak azt, mely napokra készült csomag, és mi a lenyomata.
# Minden új lenyomat után fut, tehát a napi futás automatikusan frissíti.
name: Status page

on:
  push:
    branches: [main]
    paths:
      - "manifests/**"
      - "pipeline/status/**"
      - ".github/workflows/status.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@bec219d24cd3e171d82865faccec33120bb574f4 # v10.1.0
        with:
          enable-cache: true
      - name: Az oldal előállítása
        run: uv run python -m pipeline.status.build
      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6.0.0
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5.0.0
        with:
          path: site
      - id: deploy
        uses: actions/deploy-pages@368f82528645a54fb793d4d04e342629a3f51346 # v5.0.1
